name: Terraform Dev Deployment

on:
  workflow_dispatch:
  pull_request:
    branches:
      - master
      - 'feature/*'
  push:
    branches:
      - master
      - 'feature/*'

permissions:
  id-token: write
  contents: read

jobs: 
  terraform-init-plan:
    runs-on: [self-hosted, Windows, X64, chacha-runner]

    steps: 
      - name: Checkout
        uses: actions/checkout@v5.0.0
    
      - name: Azure Login
        uses: Azure/login@v2.3.0
        with:
          client-id: 1c4d76ba-a9aa-4a2d-a48f-d46ddc33164c
          tenant-id: de80a58f-1610-4908-a1a7-862be272a38c
          subscription-id: 0f48ce1f-16d2-42ba-8b4f-0bef1a58b563

      - name: Terraform fmt
        run: terraform fmt -check
        continue-on-error: true
        working-directory: ./enviroments/dev

      - name: Terraform Init
        run: terraform init -input=false
        working-directory: ./enviroments/dev

      - name: Terraform Validate
        run: terraform validate -no-color
        working-directory: ./enviroments/dev

      - name: Terraform Plan
        run: terraform plan -no-color -input=false
        continue-on-error: true
        working-directory: ./enviroments/dev
        
  terraform-apply:    
    runs-on: [self-hosted, Windows, X64, chacha-runner]
    if: github.ref == 'refs/heads/master'
    needs: terraform-init-plan
    environment: dev
    steps:  
      - name: Checkout
        uses: actions/checkout@v5.0.0
    
      - name: Azure Login
        uses: Azure/login@v2.3.0
        with:
            client-id: 1c4d76ba-a9aa-4a2d-a48f-d46ddc33164c
            tenant-id: de80a58f-1610-4908-a1a7-862be272a38c
            subscription-id: 0f48ce1f-16d2-42ba-8b4f-0bef1a58b563

      - name: Terraform Init
        run: terraform init -input=false  
        working-directory: ./enviroments/dev

      - name: Terraform Apply
        run: terraform apply --auto-approve
        continue-on-error: true
        working-directory: ./enviroments/dev
